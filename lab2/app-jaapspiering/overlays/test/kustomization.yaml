apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
nameSuffix: -test
resources:
  - ../../base
patches:
  - target:
      name: jaapspiering-lab2-pod
      kind: Pod
    patch: |-
      - op: replace
        path: "/spec/containers/0/env/-"
        valiue: 
          name: OUTPUT
          value: "Test"
