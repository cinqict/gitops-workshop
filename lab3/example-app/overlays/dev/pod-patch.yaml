apiVersion: v1
kind: Pod
metadata:
  name: example-lab3
  labels:
    app: example-app-dev
spec:
  containers:
  - name: example-app-lab3
    image: bahqiplor/cinq-gitops-workshop:latest
    ports:
    - containerPort: 5000
    env:
      - name: OUTPUT
        value: "This is the Development environment"
      - name: LAB3_OUTPUT
        valueFrom:
          secretKeyRef:
            name: example-secret-lab3
            key: secret